import {
  timeHeader
} from 'booker-manager/helpers/time-header';

module('TimeHeaderHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = timeHeader(42);
  ok(result);
});
