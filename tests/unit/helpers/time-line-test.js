import {
  timeLine
} from 'booker-manager/helpers/time-line';

module('TimeLineHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = timeLine(42);
  ok(result);
});
